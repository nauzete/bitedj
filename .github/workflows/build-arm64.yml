name: Build Ubuntu 24.04 ARM64

on:
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 90
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build BiteDJ
        run: bash tools/build-bitedj-arm64.sh

      - name: Upload ARM64 Debian package
        uses: actions/upload-artifact@v4
        with:
          name: bitedj-ubuntu-24.04-arm64-deb
          path: dist-arm64/bitedj_1.0.0_arm64.deb
          if-no-files-found: error
